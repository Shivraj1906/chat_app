# CS6008 Secure Chat Assignment

This repository contains independently buildable implementations for each assignment phase.

Build a phase from its directory:

```sh
make -C phase1
make -C phase2
make -C phase3
make -C phase4
make -C phase5
```

See each phase directory's `README.md` for its runtime instructions. Generated binaries and private certificate material are ignored by Git.
